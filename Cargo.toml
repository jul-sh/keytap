[workspace]
members = ["core", "cli", "cli/spec", "macos", "web/wasm", "envtap"]
resolver = "2"

# The released keytap version. `keytap --version` and the CLI crates inherit
# it from here; bump this (only here) when cutting a release.
[workspace.package]
version = "10.0.0"
