[package]
name = "tapkey"
version = "1.1.0"
edition = "2021"

[[bin]]
name = "tapkey"
path = "src/main.rs"

[dependencies]
tapkey-core = { path = "../core" }
clap = { version = "4", features = ["derive"] }
base64 = "0.22"

[build-dependencies]
swift-rs = { version = "1.0", features = ["build"] }
